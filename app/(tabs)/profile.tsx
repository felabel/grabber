/**
 * Profile Screen
 * 
 * User profile and account management.
 * TODO: Implement profile UI with navigation to other screens
 */

import { View, Text, ScrollView } from 'react-native';

export default function ProfileScreen() {
  return (
    <ScrollView>
      <View>
        <Text>Profile Screen - UI to be implemented</Text>
        {/* 
          TODO: Add:
          - User profile header (avatar, name, email)
          - Orders section (link to orders list)
          - Addresses section (link to addresses screen)
          - Payment methods section
          - Settings section (link to settings screen)
          - Logout button
        */}
      </View>
    </ScrollView>
  );
}

